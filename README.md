This repository is a minimal representation of a multi-remote microfrontend setup using Vite and @module-federation/vite plugin.

**Install & Start All Apps**

At the repository root:

```
npm run setup
```

App will load at http://localhost:3000/
